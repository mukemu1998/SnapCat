<Window x:Class="SnapCat.App.Windows.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="结果详情"
        Icon="{DynamicResource Theme.Image.AppLogo}"
        Width="920"
        Height="760"
        MinWidth="760"
        MinHeight="560"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.TextPrimary}" />
        </Style>

        <Style x:Key="ResultThumbStyle" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Margin="1"
                                Background="{DynamicResource Theme.Brush.ScrollThumb}"
                                CornerRadius="4" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ResultScrollBarStyle" TargetType="ScrollBar">
            <Setter Property="Width" Value="2" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="Transparent"
                                CornerRadius="3">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand"
                                                  Focusable="False"
                                                  Opacity="0" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ResultThumbStyle}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand"
                                                  Focusable="False"
                                                  Opacity="0" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Height" Value="2" />
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource Theme.Brush.SurfaceBackground}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.ButtonBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.TextPrimary}" />
            <Setter Property="CaretBrush" Value="{DynamicResource Theme.Brush.TextPrimary}" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="VerticalContentAlignment" Value="Top" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Theme.Brush.ButtonBackground}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.ButtonBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.TextPrimary}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.78" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="ResultExpanderHeaderTemplate">
            <TextBlock Foreground="{DynamicResource Theme.Brush.TextPrimary}"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Text="{Binding}" />
        </DataTemplate>

    </Window.Resources>

    <Border x:Name="WindowBorder"
            Padding="14"
            Background="{DynamicResource Theme.Brush.WindowBackground}"
            BorderBrush="{DynamicResource Theme.Brush.WindowBorder}"
            BorderThickness="1"
            CornerRadius="18">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20"
                              ShadowDepth="5"
                              Opacity="0.26"
                              Color="{DynamicResource Theme.Color.Shadow}" />
        </Border.Effect>

        <Grid x:Name="ContentRoot">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Background="Transparent"
                  MouseLeftButtonDown="Header_OnMouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock x:Name="TitleTextBlock"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource Theme.Brush.TextPrimary}"
                               Text="结果详情" />
                    <TextBlock x:Name="StatusTextBlock"
                               Margin="0,6,0,0"
                               Foreground="{DynamicResource Theme.Brush.TextMuted}"
                               TextWrapping="Wrap" />
                </StackPanel>

                <Button Grid.Column="1"
                        Width="30"
                        Height="30"
                        Margin="12,0,0,0"
                        Background="{DynamicResource Theme.Brush.ButtonBackground}"
                        BorderBrush="{DynamicResource Theme.Brush.ButtonBorder}"
                        Click="CloseButton_OnClick"
                        Content="×"
                        FontSize="14"
                        Foreground="{DynamicResource Theme.Brush.TextPrimary}" />
            </Grid>

            <Expander x:Name="PreviewExpander"
                      Grid.Row="1"
                      Margin="0,16,0,16"
                      HeaderTemplate="{StaticResource ResultExpanderHeaderTemplate}"
                      Header="截图预览"
                      IsExpanded="False"
                      Visibility="Collapsed">
                <Border Padding="12"
                        Background="{DynamicResource Theme.Brush.CardBackground}"
                        BorderBrush="{DynamicResource Theme.Brush.CardBorder}"
                        BorderThickness="1"
                        CornerRadius="14">
                    <Grid>
                        <TextBlock x:Name="PreviewUnavailableTextBlock"
                                   Foreground="{DynamicResource Theme.Brush.TextSecondary}"
                                   Text="截图文件不可用。"
                                   Visibility="Collapsed" />
                        <ScrollViewer x:Name="PreviewScrollViewer"
                                      HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto"
                                      Visibility="Collapsed">
                            <ScrollViewer.Resources>
                                <Style TargetType="ScrollBar"
                                       BasedOn="{StaticResource ResultScrollBarStyle}" />
                            </ScrollViewer.Resources>
                            <Image x:Name="PreviewImage"
                                   Stretch="None"
                                   SnapsToDevicePixels="True"
                                   UseLayoutRounding="True" />
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Expander>

            <Grid Grid.Row="2"
                  Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel x:Name="PrimaryPanel">
                    <TextBlock x:Name="PrimaryHeaderTextBlock"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource Theme.Brush.Highlight}"
                               Text="原文" />
                    <TextBox x:Name="PrimaryTextBox"
                             Margin="0,10,0,0"
                             IsReadOnly="True"
                             VerticalScrollBarVisibility="Auto">
                        <TextBox.Resources>
                            <Style TargetType="ScrollBar"
                                   BasedOn="{StaticResource ResultScrollBarStyle}" />
                        </TextBox.Resources>
                    </TextBox>
                </StackPanel>

                <StackPanel x:Name="SecondaryPanel"
                            Grid.Column="2">
                    <TextBlock x:Name="SecondaryHeaderTextBlock"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource Theme.Brush.Highlight}"
                               Text="译文" />
                    <TextBox x:Name="SecondaryTextBox"
                             Margin="0,10,0,0"
                             IsReadOnly="True"
                             VerticalScrollBarVisibility="Auto">
                        <TextBox.Resources>
                            <Style TargetType="ScrollBar"
                                   BasedOn="{StaticResource ResultScrollBarStyle}" />
                        </TextBox.Resources>
                    </TextBox>
                </StackPanel>
            </Grid>

            <Expander x:Name="DebugExpander"
                      Grid.Row="3"
                      Margin="0,0,0,16"
                      HeaderTemplate="{StaticResource ResultExpanderHeaderTemplate}"
                      Header="OCR 调试信息"
                      IsExpanded="False"
                      Visibility="Collapsed">
                <TextBox x:Name="DebugTextBox"
                         MinHeight="160"
                         IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto">
                    <TextBox.Resources>
                        <Style TargetType="ScrollBar"
                               BasedOn="{StaticResource ResultScrollBarStyle}" />
                    </TextBox.Resources>
                </TextBox>
            </Expander>

            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Width="108"
                        Height="32"
                        Margin="0,0,12,0"
                        Click="CopyPrimaryButton_OnClick">复制左侧内容</Button>
                <Button Width="108"
                        Height="32"
                        Margin="0,0,12,0"
                        Click="CopySecondaryButton_OnClick">复制右侧内容</Button>
                <Button x:Name="CopyImagePathButton"
                        Width="108"
                        Height="32"
                        Margin="0,0,12,0"
                        Click="CopyImagePathButton_OnClick"
                        Visibility="Collapsed">复制截图路径</Button>
                <Button x:Name="CopyDebugButton"
                        Width="108"
                        Height="32"
                        Margin="0,0,12,0"
                        Click="CopyDebugButton_OnClick"
                        Visibility="Collapsed">复制调试信息</Button>
                <Button Width="96"
                        Height="32"
                        Click="CloseButton_OnClick">关闭</Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
